# SigongCore 서버 배포 설정
# cp .env.deploy.example .env.deploy 후 실제 값 입력

# SSH 접속 정보
SERVER=ubuntu@your-server-ip
SSH_PORT=22

# 서버의 sigongcore 프로젝트 경로
REMOTE_DIR=/home/ubuntu/sigongcore

# 프론트엔드 빌드 시 API URL (빌드 타임 주입)
NEXT_PUBLIC_API_URL=https://your-domain.com/api/v1
